{
  "name": "wedding-countdown",
  "version": "0.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "decrypt_conf": "openssl cast5-cbc -d -in config/secrets.json.cast5 -out config/secrets.json",
    "encrypt_conf": "openssl cast5-cbc -e -in config/secrets.json -out config/secrets.json.cast5"
  },
  "author": "John K. Paul",
  "license": "Don't use this",
  "dependencies": {
    "twilio": "^2.2.1",
    "bluebird": "^2.9.30"
  }
}
